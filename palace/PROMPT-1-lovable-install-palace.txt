Add the "Braindump Palace" to my AI Hub, and change nothing else. Do not redesign or touch any existing app.

Pull these from https://github.com/fayamonkey/AgentHub-Template (branch: braindump-palace) and add them:

1. supabase/functions/submit-braindump/index.ts  — add this edge function and DEPLOY it. It writes a new
   braindump file into my vault repo using the existing GITHUB_VAULT_TOKEN secret (the same token get-cards
   uses to read). No new secret needed.

2. src/views/BraindumpInput.jsx  — add this file exactly as-is.

3. In src/App.jsx, make exactly these additions and nothing else:
   - add the import:  import BraindumpInput from "./views/BraindumpInput.jsx";
   - add this as the FIRST item of the CATEGORIES array:
     { id: "braindump", label: "Braindump", emoji: "🧠", blurb: "Dump a thought, get it back sorted" },
   - immediately after the line  <p className="sub">Everything my AI builds for me, all in one place.</p>
     add this line:  {tab === "braindump" && <BraindumpInput />}

4. Append the Braindump CSS lines (from src/styles.css on that branch) to my src/styles.css.

Then build and show the preview. A "🧠 Braindump" tab appears in the top menu with a text box and a mic
button. Braindump cards will show under it. Everything else stays exactly as it was.
